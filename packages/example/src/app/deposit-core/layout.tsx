import React from 'react';
import WebLoginAndAntdLayout from '@/pageComponents/WebLoginAndAntdLayout';

const WithdrawCoreLayout = ({ children }: { children: React.ReactNode }) => {
  return <WebLoginAndAntdLayout>{children}</WebLoginAndAntdLayout>;
};

export default WithdrawCoreLayout;
